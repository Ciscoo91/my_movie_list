<div class="nav flex-row justify-content-between align-items-center bg-dark py-1">
    <div class="logo text-light ml-3 display-2">
        ML
    </div>
    <ul class="navbar list-inline d-flex flex-row justify-content-around mr-3">
        <li class="nav-item">
            <a class="nav-link text-light active" aria-current="page" href="{{path('app_home')}}">Home</a>
        </li>
        {% if app.user %}
        <li>
            <a class="nav-link text-light" href="{{path('app_profile')}}">Profile</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-light" href="{{path('app_logout')}}">Logout</a>
        </li>
        <li class="nav-item">
            <a href="{{path('app_movies')}}" class="nav-link text-light">Movies</a>
        </li>
        {% else %}
        <li>
            <a class="nav-link text-light" href="{{path('app_login')}}">Login</a>
        </li>
        <li>
            <a class="nav-link text-light" href="{{path('app_register')}}">Register</a>
        </li>
        {% endif %}
        {% if app.user and is_granted('ROLE_ADMIN') %}
        <li>
            <a class="nav-link text-light" href="{{path('admin')}}">Admin</a>
        </li>
        {% endif %}
    </ul>
</div>